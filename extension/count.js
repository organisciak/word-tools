// Generated by CoffeeScript 1.4.0
(function() {
  var dfdgetWordCount, simpleTokenizer;

  dfdgetWordCount = function(e, t, tokenizer) {
    var text, words;
    if (tokenizer == null) {
      tokenizer = simpleTokenizer;
    }
    text = e.selectionText;
    words = tokenizer(text);
    if (text.length === 0) {
      return console.log("No words selected");
    } else {
      return console.log("" + words + " word" + (words > 1 ? 's' : '') + " selected");
    }
  };

  simpleTokenizer = function(text) {
    return text.split(" ").length;
  };

  chrome.contextMenus.create({
    "title": "Word Count",
    "contexts": ["selection"],
    "onclick": getWordCount
  });

}).call(this);
